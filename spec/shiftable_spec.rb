# frozen_string_literal: true

RSpec.describe Shiftable do
  it "has a version number" do
    expect(Shiftable::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
